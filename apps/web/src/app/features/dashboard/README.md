# Dashboard Feature Plan

Recommended Angular feature modules:

- `dashboard-shell`: route container and user layout loader
- `widget-host`: dynamic widget renderer (chart, KPI, table, news)
- `layout-editor`: drag/drop, resize, save layout to API
- `widget-config`: symbol/timeframe/metric editing
- `stream-client`: WebSocket subscriptions and state updates
